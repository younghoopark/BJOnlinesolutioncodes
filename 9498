#include <iostream>

using namespace std;

int main()
{
	int point;
	cin >> point;
	if (point >= 90 && point <= 100)
	{
		cout << 'A' << endl;
	}
	else if (point >= 80 && point <= 89)
	{
		cout << 'B' << endl;
	}
	else if (point >= 70 && point <= 79)
	{
		cout << 'C' << endl;
	}
	else if (point >= 60 && point <= 69)
	{
		cout << 'D' << endl;
	}
	else
	{
		cout << 'F' << endl;
	}

	return 0;
}
